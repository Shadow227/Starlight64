{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Win64Debug",
            "program": "${workspaceFolder}/starlight 64.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}